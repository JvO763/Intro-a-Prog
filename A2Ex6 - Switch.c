#include <stdio.h>

int main(){
	int op;
	scanf("%c",&op);
	
		switch(op){
			case'1': printf("oi"); break;
			case'2': printf("%c", op); break;
			case'3': printf("%c", op); break;
			case'4': printf("%c", op); break;
		}
}
